doctype html
html(lang="ko")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title 웹디자이너 선영훈 포트폴리오
    include includes/comm_style.pug
  body 
    include includes/comm_header.pug
    h1(class="d-none") contactList
    main
      hgroup(class="text-center py-2")
        h2(class="caps") contact &amp; request Lists
        h3 작업요청 및 문의 리스트
      article 
        h2(class="d-none") main - contactList
        div(class="container mx-auto")
          table(class="contact_list") 
            tr 
              th(class="th-5") 번호
              th(class="th-5") 구분
              th(class="th-5") 이름
              th(class="th-10") 연락처
              th(class="th-10") 이메일
              th 제목
              th 내용
              th 등록일
              th(class="th-5") 관리
            each data in dataset
              tr 
                td= data.id 
                td= data.gubun 
                td= data.name 
                td= data.phone 
                td= data.email 
                td= data.title 
                td= data.memo 
                td= data.regdate
                td
                  button(onclick=`location.href='/contactDel?id=${data.id}'`) 삭제

    include includes/comm_footer.pug